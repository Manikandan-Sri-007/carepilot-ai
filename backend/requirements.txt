# Direct application dependencies only. Transitive packages (especially
# Starlette) are resolved by FastAPI so their versions remain compatible.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
SQLAlchemy>=2,<3
passlib[bcrypt]>=1.7.4,<2
# Passlib 1.7.4 is not compatible with bcrypt 5's changed hashpw behavior.
bcrypt==4.0.1
python-jose[cryptography]>=3.3,<4
email-validator>=2,<3
python-multipart>=0.0.9,<1
# Required by FastAPI/Starlette's TestClient for API integration tests.
httpx>=0.27,<1
